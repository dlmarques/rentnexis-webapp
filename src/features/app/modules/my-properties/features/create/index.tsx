import CreatePropertyForm from "./components/Form";

const CreateMyProperties = () => {
  return (
    <div className="w-full overflow-y-auto overflow-x-hidden flex justify-center">
      <CreatePropertyForm />
    </div>
  );
};

export default CreateMyProperties;
